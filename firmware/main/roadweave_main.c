#include "esp_log.h"

static const char *TAG = "roadweave";

void app_main(void)
{
    ESP_LOGI(TAG, "RoadWeave P0 scaffold");
    ESP_LOGW(TAG, "HaLow is not enabled until the HC01 V2 BCF/FW gate is verified");
}

