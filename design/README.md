# RoadWeave UI/UXデザイン

- [web-poc](web-poc/README.md): Circle・Pulse・Compassの3案を比較するWeb PoC。参加・退出、PTT、音量・消音、位置表示を仮データで試せます。

リポジトリ直下から `cd design/web-poc` で移動し、`npm ci` と `npm run dev -- --host 127.0.0.1` で起動します。検証手順とLVGLへの移植方針はWeb PoCのREADMEを参照してください。
